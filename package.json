{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/eoacoustic-site dev",
    "build": "pnpm --filter @workspace/eoacoustic-site build",
    "serve": "pnpm --filter @workspace/eoacoustic-site serve",
    "preview": "pnpm --filter @workspace/eoacoustic-site serve",
    "typecheck": "pnpm --filter @workspace/eoacoustic-site typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
